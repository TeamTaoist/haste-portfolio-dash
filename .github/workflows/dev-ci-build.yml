name: trigger-netlify-build | Main Branch

on:
  push:
    branches: [ dev ]

jobs:             
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Build
        run: |
          npm install
          npm run build
